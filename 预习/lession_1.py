from pixfog import *

forward(101)
right(120)
forward(101)
right(120)
forward(101)
right(120)




# 一定要加上这句话
pause()


# 1
# 实现函数, 用于画一个正方形, 边长由参数提供
# 参数 x, y 是正方形左上角坐标
# 参数 l(注意，这里是字母 l，不是数字 1) 是正方行边长
# 函数声明如下
# def square(x, y, l):
#     jump(x, y)
#     i = 0
#     while i < 4:
#         forward(l)
#         right(90)
#         i = i + 1


# square(0, 0, 100)
# square(-100, -50, 75)